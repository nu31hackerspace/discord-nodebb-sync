{
  "name": "nodebb-plugin-discord-sync",
  "version": "0.1.0",
  "description": "NodeBB import endpoint for discord-nodebb-sync",
  "main": "library.js",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "files": [
    "library.js",
    "plugin.json",
    "lib"
  ],
  "scripts": {
    "test": "npm --prefix worker test",
    "import": "npm --prefix worker run import",
    "sync": "npm --prefix worker run sync",
    "fixture": "npm --prefix worker run fixture"
  },
  "nbbpm": {
    "compatibility": "^4.15.0"
  }
}
